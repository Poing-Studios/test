name: Minha primeira action

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run a command
        run: echo >> test
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16.x'
      - name: Bump version and commit changes
        run: |
          git config --global user.email "poingstudios@gmail.com"
          git config --global user.name "Poing Studios[bot]"
          git add .
          git commit -m "Bump version"
          git push

      - name: Create new Git tag
        run: |
          git tag -a v1$(node -p "require('./package.json').version") -m "Version $(node -p "require('./package.json').version")"
          git push origin --tags
